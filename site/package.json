{
  "private": true,
  "type": "module",
  "description": "Compiles and lints the Projector site's TypeScript. The compiled JavaScript is committed, because every repository's deploy and every CLI install reads it from a Git tree.",
  "scripts": {
    "build": "tsc",
    "lint": "eslint --max-warnings 0 src",
    "check": "tsc --noEmit && eslint --max-warnings 0 src"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "eslint": "10.11.0",
    "typescript": "6.0.3",
    "typescript-eslint": "8.70.1"
  }
}
